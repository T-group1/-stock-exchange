get:
  operationId: getRateHistory
  summary: История курса для валютной пары
  description: |
    Возвращает исторические данные для построения графика.
    Поддерживает прямые пары (USD/RUB) и кросс-курсы (EUR/USD).
  parameters:
    - name: pair
      in: path
      description: Валютная пара в формате BASE_QUOTE
      required: true
      schema:
        type: string
      example: EUR_USD
    - name: period
      in: query
      description: Масштаб времени для графика
      required: false
      schema:
        type: string
        default: 1m
        enum:
          - 1d
          - 1w
          - 1m
          - 3m
    - name: fill_gaps
      in: query
      description: Заполнять пропуски (выходные/праздники) значением предыдущего дня
      required: false
      schema:
        type: boolean
        default: false
  responses:
    '200':
      description: Исторические данные
      content:
        application/json:
          schema:
            $ref: ../components/schemas/RateHistoryResponse.yaml
    '400':
      $ref: ../components/responses/ValidationError.yaml
    '404':
      description: Валютная пара не найдена
      content:
        application/json:
          schema:
            $ref: ../components/schemas/Error.yaml
    '500':
      $ref: ../components/responses/ServerError.yaml
  security: []
  tags:
    - rates
