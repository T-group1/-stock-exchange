delete:
  tags: [user]
  operationId: removeFavorite
  summary: Удалить пару из избранного
  security:
    - CookieAuth: []
  parameters:
    - name: pair
      in: path
      required: true
      schema:
        type: string
      example: USD_RUB
  responses:
    "204":
      description: Пара удалена из избранного
    "401":
      $ref: ../components/responses/UnauthorizedError.yaml
