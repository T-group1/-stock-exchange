type: object
properties:
  from:
    type: string
    example: EUR
  to:
    type: string
    example: USD
  amount:
    type: number
    format: double
    example: 100
    minimum: 0.01
  date:
    description: Дата для исторической конвертации (Unix timestamp в секундах, null = текущий курс)
    type: integer
    format: int64
    example: 1718100000
    nullable: true
required:
  - from
  - to
  - amount
