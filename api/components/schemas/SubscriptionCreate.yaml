type: object
properties:
  currency_pair:
    type: string
    example: USD/RUB
  rate_value:
    type: number
    format: double
  condition:
    type: string
    enum:
      - above
      - below
required:
  - currency_pair
  - rate_value
  - condition
