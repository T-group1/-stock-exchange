type: object
properties:
  id:
    type: string
    format: uuid
  user_id:
    type: string
    format: uuid
  currency_pair:
    type: string
    example: USD/RUB
  rate_value:
    type: number
    format: double
  condition:
    type: string
    enum:
      - above
      - below
  is_active:
    type: boolean
    default: true
  created_at:
    description: Unix timestamp в секундах
    type: integer
    format: int64
    example: 1718100000
  triggered_at:
    description: Время срабатывания уведомления (Unix timestamp в секундах)
    type: integer
    format: int64
    example: 1718150000
    nullable: true
required:
  - id
  - user_id
  - currency_pair
  - rate_value
  - condition
  - created_at
