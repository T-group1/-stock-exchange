version: 2
sql:
  - engine: postgresql
    schema: "init.sql"
    queries: "sql/queries"
    gen:
      go:
        package: db
        out: internal/db
        sql_package: pgx/v5
        emit_interface: true
        emit_pointers_for_null_types: true
